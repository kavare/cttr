---
import Layout from '../../../layouts/Layout.astro';
import StarQuestions from '../../../components/exercises/StarQuestions';
import { t } from '../../../i18n/translations';

const lang = 'en';
const i = t(lang);
---

<Layout title={i.exercises.starQuestions.title} lang={lang}>
  <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
    <h1 class="text-3xl font-bold text-gray-900 mb-2">{i.exercises.starQuestions.title}</h1>
    <p class="text-gray-600 mb-8">{i.exercises.starQuestions.description}</p>
    <StarQuestions lang={lang} client:load />
  </section>
</Layout>
